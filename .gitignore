/target/
/.biscuits/
/.terminal_ai_agent/
/biscuit/

# macOS Finder metadata — never commit, anywhere in the tree.
.DS_Store
**/.DS_Store

# Local environment / secrets — never commit.
.env
.env.local
.env.*.local

# Zip archives (e.g. biscuit-browser.zip) are build artifacts, never committed.
*.zip
